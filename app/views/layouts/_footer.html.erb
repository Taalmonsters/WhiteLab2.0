	</div>
	<%= render partial: "layouts/dialogs" %>
	<%= render partial: "layouts/tour" if !@tour_title.blank? %>
	<script>
		start_tour = <%= @tour_start ? 'true' : 'false' %>;
		Whitelab.namespace = '<%= @namespace %>';
		Whitelab.metadata.filterTokenSafeLimit = <%= FILTER_TOKEN_SAFE_LIMIT %>;
	</script>
</body>
</html>